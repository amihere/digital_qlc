defmodule QlcDigital.QuestionList do
  """
    First, user has an id
    They have a series of questions
    Each question has a next question till the end
  """

  def get_question(q_id) do
  end
end
